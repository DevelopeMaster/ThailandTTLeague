<!DOCTYPE html>
<html lang="<%= lang %>">
<head>
    <meta charset="UTF-8">
    <meta property="og:title" content="My Table Tennis Achievement!" />
    <meta property="og:description" content="<%= name %> has improved their rating by <%= ratingChange %> points!" />
    <meta property="og:image" content="<%= image %>" />
    <meta property="og:url" content="<%= userPageLink %>" />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= name %>'s Table Tennis Result</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            text-align: center;
            background: #f8f8f8;
            margin: 0;
            padding: 20px;
        }
        img {
            max-width: 100%;
            height: auto;
            border-radius: 20px;
        }
        .container {
            max-width: 600px;
            margin: auto;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>🏓 <%= name %>'s Achievement</h1>
        <p>Rating Change: <strong><%= ratingChange %></strong></p>
        <a href="<%= userPageLink %>" target="_blank">
            <!-- <img src="<%= image %>" alt="Result Image"> -->
        </a>
        <p><a href="<%= userPageLink %>" target="_blank">View Full Profile</a></p>
    </div>
</body>
</html>
